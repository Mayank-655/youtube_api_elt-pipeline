[pytest]
pythonpath = dags
markers =
    unit: fast tests with mocks
    integration: live API or Postgres
    functional: warehouse helpers without running Airflow
    e2e: Airflow CLI and full local stack assumptions
